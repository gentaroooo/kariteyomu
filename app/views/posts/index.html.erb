<% if logged_in? %>
  <div class="container">
   <p><div class="row row-cols-1 row-cols-md-2"></p>
      <% if @posts.present? %>
        <%= render @posts %>
      <% else %>
        <p><%= t('.no_result') %></p>
      <% end %>
    </div>
     <%= paginate @posts %> 
   </div>
 </div>
<% else %>
  <div class="top-wrapper">
  <div class="top-inner-text">
    <h1>かりてよむ</h1>
  </div>
</div>
<% end %>
