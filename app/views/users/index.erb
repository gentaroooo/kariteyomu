<% content_for(:title, t('.title')) %>
<div class="container">
  <div class="text-center top-text p-1">ユーザーリスト</div>
    <div class="row row-cols-1 row-cols-md-2">
      <%= render @users %>
    </div>
    <div class="mx-auto">
      <%= paginate @users %>
    </div>
</div>
