<div class="text-center top-text p-1">レビュー</div>
<% content_for(:title, t('.title')) %>
  <div class="container">
    <div class="row row-cols-1 row-cols-md-2"></p>
      <% if @like_posts.present? %>
        <%= render @like_posts %>
      <% else %>
        <p><%= t('.no_result') %></p>
      <% end %>
    </div>
    <%= paginate @like_posts %>
  </div>
</div>
