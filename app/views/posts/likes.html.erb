<% if @like_posts.present? %>
  <div class="text-center top-text p-1">いいね</div>
    <div class="container">
      <div class="row row-cols-1 row-cols-md-2">
        <%= render @like_posts %>
      </div>
    <%= paginate @like_posts %>
    </div>
  </div>
<% else %>
  <h3 class="container text-center top-text p-3"><%= t('.no_result') %></h3>
<% end %>
