<div class="text-center top-text p-1">読みたい</div>
<div class="container">
  <div class="row row-cols-1 row-cols-md-2">
    <% if @book.present? %>
      <%= render @book %>
    <% else %>
      <p><%= t('.no_result') %></p>
    <% end %>
    </div>
  </div>
  <%= paginate @book %> 
</div>